apiVersion: resourcemanager.cnrm.cloud.google.com/v1beta1
kind: Folder
metadata:
  name: top-folder
  namespace: xxx # kpt-set: ${company}-${dept}-${project}
spec:
  displayName: xxx # kpt-set: ${company}-${dept}-${project}
  folderRef:
    external: "994625547552"